# Language identification evaluation

The `eval` directory should contain the following subdirectories:

- `phylaudio2` (or your project name): contains run checkpoints for each W&B run
- `wandb`: contains W&B logs
