# Language identification evaluation

The `eval` directory should contain the following subdirectory:

- `phylaudio` (or your project name): contains run checkpoints for each W&B run
- `wandb`: contains W&B logs
- `summary.json`: a summary of the main metrics of each run
